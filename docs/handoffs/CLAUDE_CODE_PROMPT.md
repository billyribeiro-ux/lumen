# Claude Code — Continue Building Lumen

Read `docs/handoffs/2026-04-24-phase-0-handoff.md` in full (including the ADDENDUM at the bottom). That is your brief.

Then execute the entire remaining build autonomously:

1. Finish Phase 0: create ADR-002 through ADR-010 and `docs/adr/README.md`.
2. Execute Phase 1 through Phase 18 in strict topological order per the brief.
3. After every `git commit`, append a row to `docs/BUILD_LOG.md` per the ADDENDUM rules. No exceptions.
4. Tag and release at every phase milestone. Update `CHANGELOG.md` and `ROADMAP.md`.
5. Follow Conventional Commits with `Phase: N` and `Refs: PE7-<AREA>` footers.
6. Do not ask permission for obvious technical steps. Execute and move on.
7. Only stop for product decisions: branding, copy, pricing, domains, or anything needing Billy's external account access.

Start with ADR-002. Go.
